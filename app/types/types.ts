export enum GameState {
    IDLE = 'idle',
    PLAYING = 'playing',
    PAUSED = 'paused',
    ENDED = 'ended'
  }

  export enum Timer {
    RUNNING = 'running',
    PAUSED = 'paused',
    STOPPED = 'stopped'
  }